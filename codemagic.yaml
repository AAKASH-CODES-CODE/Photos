workflows:
  my-flutter-workflow:
    name: Build Android APK
    working_directory: photovault_flutter
    instance_type: mac_mini_m1
    max_build_duration: 120
    environment:
      flutter: stable
    scripts:
      - name: Fix Android v1 Embedding (Auto-Upgrade)
        script: |
          rm -rf android
          flutter create --platforms=android .
      - name: Bulletproof Android Setup
        script: |
          echo "org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8" >> android/gradle.properties
          cat << 'EOF' >> android/build.gradle
          
          subprojects {
              afterEvaluate { project ->
                  if (project.hasProperty('android')) {
                      project.android {
                          compileSdkVersion 36
                          lintOptions {
                              checkReleaseBuilds false
                              abortOnError false
                          }
                      }
                  }
              }
          }
          EOF
      - name: Get Flutter Packages
        script: flutter pub get
      - name: Build Android APK
        script: flutter build apk --release --verbose
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      
